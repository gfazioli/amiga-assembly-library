'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'orange',
  fontFamilyMonospace: '"Topaz a1200", "Fira Code", "JetBrains Mono", monospace',
});
