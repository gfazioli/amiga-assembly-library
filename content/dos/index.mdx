# DOS Module

The DOS module provides file I/O and disk utility functions that simplify common operations built on top of the Amiga's `dos.library`. These functions handle file loading, saving, information retrieval, error checking, checksum calculation, and console input.

## File I/O Functions

- **[Load](/docs/dos/load)** — Loads a file from disk into memory, with optional automatic buffer allocation.
- **[Save](/docs/dos/save)** — Writes a buffer to a file or device.
- **[LineInput](/docs/dos/line-input)** — Reads keyboard input through a file handle.

## File Information Functions

- **[FileInfo](/docs/dos/file-info)** — Retrieves a `FileInfoBlock` structure with information about a file.
- **[CheckFile](/docs/dos/check-file)** — Checks the status of a file and optionally returns a localized error string.
- **[UnitInfo](/docs/dos/unit-info)** — Retrieves an `InfoData` structure with information about a volume or device.

## Data Integrity Functions

- **[CheckSum](/docs/dos/check-sum)** — Calculates various types of checksums (BootBlock, DataBlock, FileBlock).
